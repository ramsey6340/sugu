/*
class Country {
  final String name; // Nom du pays
  final String flag; // Le drapeau du pays
  final String code; // Le code d'abréviation du nom du pays
  final String dialCode; // Le code numéro du pays, par exemple 223 pour le Mali
  final int minLength; // la taille minimal du numéro du pays
  final int maxLength; // la taille maximal du numéro du pays

  // Constructeur de base
  Country({
    required this.name,
    required this.flag,
    required this.code,
    required this.dialCode,
    required this.minLength,
    required this.maxLength,
  });
  
  // Constructeur vide
  const Country.isEmpty({
    this.name = '',
    this.flag = '',
    this.code = '',
    this.dialCode = '',
    this.minLength = 0,
    this.maxLength = 0,
  });

}
 */